# 复制成 .dev.vars（已 gitignore，不会被 commit、也不会被 wrangler deploy 带上去）
#
# 设了这个，跑 npm run dev 时就用这个 email 当登入身分，
# 不用先弄好网域和 Cloudflare Access。
# 只在本机生效 —— 线上的请求都带 cf-ray 标头，带了就不会套用。
DEV_BYPASS_EMAIL = "rasafoodhubplt@gmail.com"
